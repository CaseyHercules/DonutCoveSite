---
import Layout from '../layouts/Layout.astro';
---

<Layout 
  title="Contact Us - Donut Cove Consulting" 
  description="Get in touch with Donut Cove Consulting to discuss how we can help eliminate tedious tasks and make your team's work more meaningful."
>
  <!-- Hero Section with Organic Blobs -->
  <section class="relative py-32 md:py-48 overflow-hidden">
    <!-- Organic Background Blobs -->
    <div class="absolute top-0 left-0 w-full h-full overflow-hidden -z-10 pointer-events-none">
      <div class="absolute top-[-10%] left-[-10%] w-[500px] h-[500px] bg-coral-light/20 rounded-full blur-[100px] animate-pulse"></div>
      <div class="absolute bottom-[10%] right-[-5%] w-[400px] h-[400px] bg-indigo/20 rounded-full blur-[80px] animate-pulse" style="animation-delay: 2s;"></div>
    </div>

    <div class="max-w-4xl relative z-10">
      <h1 class="font-display font-black text-6xl md:text-8xl tracking-tight leading-[0.95] text-ink dark:text-ink-dark mb-8 drop-shadow-sm">
        Let's Make<br />
        <span class="text-coral-vibrant italic">Work Sweet</span>
      </h1>
      <p class="text-xl md:text-2xl text-ink/80 dark:text-ink-dark/80 max-w-2xl leading-relaxed font-medium">
        Ready to eliminate tedious tasks and help your team focus on meaningful work? We'd love to hear about your challenges and discuss how we can help.
      </p>
    </div>
  </section>

  <!-- Contact Section -->
  <section class="py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-16">
        
        <!-- Contact Information -->
        <div class="space-y-8">
          <div class="space-y-6">
            <h2 class="font-display font-bold text-3xl text-tech-charcoal">
              Get In Touch
            </h2>
            <p class="text-lg text-tech-slate leading-relaxed">
              We believe great software starts with great conversations. Let's discuss your team's pain points and explore how we can help make their work more enjoyable and meaningful.
            </p>
          </div>
          
          <!-- Contact Methods -->
          <div class="space-y-6">
            <div class="flex items-start space-x-4">
              <div class="flex-shrink-0 w-12 h-12 bg-gradient-to-br from-tech-pink to-tech-pink-dark rounded-full flex items-center justify-center">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                </svg>
              </div>
              <div>
                <h3 class="font-semibold text-lg text-tech-charcoal">Email</h3>
                <p class="text-tech-slate">Drop us a line anytime</p>
                <a href="mailto:info@donutcove.com" class="text-tech-pink-dark hover:text-tech-pink transition-colors font-medium">
                  info@donutcove.com
                </a>
              </div>
            </div>
            
            <div class="flex items-start space-x-4">
              <div class="flex-shrink-0 w-12 h-12 bg-gradient-to-br from-tech-teal to-tech-teal-dark rounded-full flex items-center justify-center">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
              </div>
              <div>
                <h3 class="font-semibold text-lg text-tech-charcoal">Response Time</h3>
                <p class="text-tech-slate">We typically respond within 24 hours</p>
                <p class="text-tech-teal-dark font-medium">
                  Usually much faster!
                </p>
              </div>
            </div>
            
            <div class="flex items-start space-x-4">
              <div class="flex-shrink-0 w-12 h-12 bg-gradient-to-br from-tech-emerald to-tech-emerald-dark rounded-full flex items-center justify-center">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8h2a2 2 0 012 2v6a2 2 0 01-2 2h-2v4l-4-4H9a2 2 0 01-2-2v-6a2 2 0 012-2h2m-4 3h12"></path>
                </svg>
              </div>
              <div>
                <h3 class="font-semibold text-lg text-tech-charcoal">Free Consultation</h3>
                <p class="text-tech-slate">No pressure, no sales pitch</p>
                <p class="text-tech-emerald-dark font-medium">
                  Just genuine problem-solving
                </p>
              </div>
            </div>
          </div>
          
          <!-- Decorative Element -->
          <div class="hidden lg:block">
            <div class="w-full h-48 bg-gradient-to-br from-tech-pink-light via-tech-teal-light to-tech-emerald-light rounded-3xl flex items-center justify-center relative overflow-hidden">
              <div class="absolute inset-0 bg-white/20 backdrop-blur-sm"></div>
              <div class="relative z-10 text-center space-y-4">
                <div class="text-6xl animate-float">🍩</div>
                <div class="text-lg font-display font-semibold text-tech-charcoal">
                  Let's Chat!
                </div>
              </div>
              <div class="absolute top-8 left-8 w-4 h-4 bg-tech-pink rounded-full animate-bounce-gentle"></div>
              <div class="absolute bottom-8 right-8 w-5 h-5 bg-tech-teal rounded-full animate-float" style="animation-delay: 1s;"></div>
            </div>
          </div>
        </div>
        
        <!-- Contact Form -->
        <div class="bg-white rounded-3xl shadow-lg p-8 lg:p-12">
          <h2 class="font-display font-bold text-2xl text-tech-charcoal mb-8">
            Tell Us About Your Project
          </h2>
          
          <form id="contact-form" action="/api/contact" method="POST" class="space-y-6">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <label for="firstName" class="block text-sm font-medium text-tech-slate mb-2">
                  First Name *
                </label>
                <input
                  type="text"
                  id="firstName"
                  name="firstName"
                  required
                  aria-required="true"
                  class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-tech-teal focus:border-tech-teal transition-colors"
                  placeholder="John"
                />
              </div>
              <div>
                <label for="lastName" class="block text-sm font-medium text-tech-slate mb-2">
                  Last Name *
                </label>
                <input
                  type="text"
                  id="lastName"
                  name="lastName"
                  required
                  aria-required="true"
                  class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-tech-teal focus:border-tech-teal transition-colors"
                  placeholder="Doe"
                />
              </div>
            </div>
            
            <div>
              <label for="email" class="block text-sm font-medium text-tech-slate mb-2">
                Email Address *
              </label>
              <input
                type="email"
                id="email"
                name="email"
                required
                aria-required="true"
                aria-invalid="false"
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-tech-teal focus:border-tech-teal transition-colors"
                placeholder="john@company.com"
              />
            </div>
            
            <div>
              <label for="company" class="block text-sm font-medium text-tech-slate mb-2">
                Company Name
              </label>
              <input
                type="text"
                id="company"
                name="company"
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-tech-teal focus:border-tech-teal transition-colors"
                placeholder="Your Company Inc."
              />
            </div>
            
            <div>
              <label for="projectType" class="block text-sm font-medium text-tech-slate mb-2">
                Project Type
              </label>
              <select
                id="projectType"
                name="projectType"
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-tech-teal focus:border-tech-teal transition-colors"
              >
                <option value="">Select a project type</option>
                <option value="automation">Process Automation</option>
                <option value="custom-app">Custom Application</option>
                <option value="integration">System Integration</option>
                <option value="consultation">Consultation</option>
                <option value="other">Other</option>
              </select>
            </div>
            
            <div>
              <label for="message" class="block text-sm font-medium text-tech-slate mb-2">
                Tell us about your challenges *
              </label>
              <textarea
                id="message"
                name="message"
                rows="5"
                required
                aria-required="true"
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-tech-teal focus:border-tech-teal transition-colors resize-none"
                placeholder="What tedious tasks are slowing down your team? What would you like to automate or improve?"
              ></textarea>
            </div>
            
            <!-- Simple Standard Checkbox -->
            <div class="space-y-3">
              <div class="flex items-start space-x-3">
                <input
                  type="checkbox"
                  id="privacy"
                  name="privacy"
                  value="agreed"
                  class="mt-1 h-4 w-4 text-tech-blue border-gray-300 rounded focus:ring-tech-teal focus:ring-2"
                />
                <label for="privacy" class="text-sm text-tech-slate cursor-pointer">
                  I agree to receive communications from Donut Cove Consulting about my inquiry. We respect your privacy and will never share your information.
                </label>
              </div>
              
              <!-- Modern validation message -->
              <div id="validation-message" class="hidden bg-gradient-to-r from-blue-50 to-purple-50 border-l-4 border-tech-blue p-4 rounded-lg" role="alert" aria-live="polite">
                <div class="flex items-center">
                  <svg class="w-5 h-5 text-tech-blue mr-3 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                    <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
                  </svg>
                  <div>
                    <h4 class="text-tech-charcoal font-semibold text-sm">Agreement Required</h4>
                    <p class="text-tech-slate text-sm">Please agree to receive communications so we can respond to your inquiry.</p>
                  </div>
                </div>
              </div>
            </div>
            
            <button
              type="submit"
              class="w-full bg-gradient-to-r from-tech-blue to-tech-purple text-white px-8 py-4 rounded-xl font-semibold text-lg hover:shadow-glow-purple transform hover:-translate-y-1 transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed disabled:transform-none"
              id="submit-btn"
            >
              <span id="button-text">Send Message</span>
              <span id="button-loading" class="hidden">
                <svg class="animate-spin -ml-1 mr-3 h-5 w-5 text-white inline" fill="none" viewBox="0 0 24 24">
                  <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                  <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                </svg>
                Sending...
              </span>
            </button>
          </form>
          
          <!-- Success Message -->
          <div id="success-message" class="hidden mt-6 p-4 bg-gradient-to-r from-blue-50 to-teal-50 border-l-4 border-tech-teal rounded-lg" role="alert" aria-live="polite">
            <div class="flex items-center">
              <svg class="w-5 h-5 text-tech-teal mr-3" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
              </svg>
              <div>
                <h4 class="text-tech-charcoal font-semibold">Message Sent!</h4>
                <p class="text-tech-slate" id="success-text">Thank you! We've received your message and will get back to you soon.</p>
              </div>
            </div>
          </div>
          
          <!-- Error Message -->
          <div id="error-message" class="hidden mt-6 p-4 bg-gradient-to-r from-purple-50 to-pink-50 border-l-4 border-tech-purple rounded-lg" role="alert" aria-live="assertive">
            <div class="flex items-center">
              <svg class="w-5 h-5 text-tech-purple mr-3" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path>
              </svg>
              <div>
                <h4 class="text-tech-charcoal font-semibold">Something went wrong</h4>
                <p class="text-tech-slate" id="error-text">Please try again or email us directly at info@donutcove.com</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Form Handling Script -->
  <script>
    function initializeContactForm() {
      const form = document.getElementById('contact-form') as HTMLFormElement;
      const submitBtn = document.getElementById('submit-btn') as HTMLButtonElement;
      const buttonText = document.getElementById('button-text');
      const buttonLoading = document.getElementById('button-loading');
      const successMessage = document.getElementById('success-message');
      const errorMessage = document.getElementById('error-message');
      const validationMessage = document.getElementById('validation-message');
      const privacyCheckbox = document.getElementById('privacy') as HTMLInputElement;
      const successText = document.getElementById('success-text');
      const errorText = document.getElementById('error-text');
      
      if (!form || !submitBtn || !buttonText || !buttonLoading || !successMessage || !errorMessage || !validationMessage || !privacyCheckbox) {
        return;
      }
      
      // Hide validation message when checkbox is checked
      privacyCheckbox.addEventListener('change', function() {
        if (privacyCheckbox.checked) {
          validationMessage!.classList.add('hidden');
        }
      });
      
      form.addEventListener('submit', async function(e) {
        e.preventDefault();
        
        // Hide previous messages
        successMessage!.classList.add('hidden');
        errorMessage!.classList.add('hidden');
        validationMessage!.classList.add('hidden');
        
        // Check privacy checkbox specifically (prevent browser validation popup)
        if (!privacyCheckbox.checked) {
          validationMessage!.classList.remove('hidden');
          privacyCheckbox.focus();
          return;
        }
        
        // Check required fields manually to avoid browser popups
        const firstName = (document.getElementById('firstName') as HTMLInputElement).value.trim();
        const lastName = (document.getElementById('lastName') as HTMLInputElement).value.trim();
        const emailInput = document.getElementById('email') as HTMLInputElement;
        const email = emailInput.value.trim();
        const message = (document.getElementById('message') as HTMLTextAreaElement).value.trim();
        
        if (!firstName || !lastName || !email || !message) {
          if (errorText) {
            errorText.textContent = 'Please fill in all required fields.';
          }
          errorMessage!.classList.remove('hidden');
          return;
        }
        
        // Validate email format
        const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
        if (!emailRegex.test(email)) {
          emailInput.setAttribute('aria-invalid', 'true');
          if (errorText) {
            errorText.textContent = 'Please enter a valid email address.';
          }
          errorMessage!.classList.remove('hidden');
          emailInput.focus();
          return;
        }
        emailInput.setAttribute('aria-invalid', 'false');
        
        // Show loading state
        submitBtn!.disabled = true;
        buttonText!.classList.add('hidden');
        buttonLoading!.classList.remove('hidden');
        
        // Create FormData from form
        const formData = new FormData(form);
        
        // Convert FormData to JSON object
        const data = {
          firstName: formData.get('firstName'),
          lastName: formData.get('lastName'),
          email: formData.get('email'),
          company: formData.get('company'),
          projectType: formData.get('projectType'),
          message: formData.get('message'),
          privacy: formData.get('privacy')
        };
        
        try {
          // Submit to Astro API endpoint
          const response = await fetch('/api/contact', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json',
            },
            body: JSON.stringify(data)
          });
          
          const result = await response.json();
          
          if (result.success) {
            // Show success message
            if (successText) {
              successText.textContent = result.message;
            }
            successMessage!.classList.remove('hidden');
            
            // Reset form
            form.reset();
          } else {
            // Show error message
            if (errorText) {
              errorText.textContent = result.error;
            }
            errorMessage!.classList.remove('hidden');
          }
          
        } catch (error) {
          console.error('Form submission error:', error);
          
          // Show error message
          if (errorText) {
            errorText.textContent = 'Something went wrong. Please try again or email us directly.';
          }
          errorMessage!.classList.remove('hidden');
        }
        
        // Reset button state
        submitBtn!.disabled = false;
        buttonText!.classList.remove('hidden');
        buttonLoading!.classList.add('hidden');
      });
    }
    
    // Initialize on page load
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initializeContactForm);
    } else {
      initializeContactForm();
    }
    
    // Re-initialize after Astro page transitions
    document.addEventListener('astro:page-load', initializeContactForm);
  </script>
</Layout> 